{
  "name": "feltui-monorepo",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "bun prettier && eslint .",
    "lint:fix": "bun prettier:fix && eslint . --fix",
    "prettier": "prettier --check .",
    "prettier:fix": "prettier --write .",
    "sv": "cd packages/svelte && bun",
    "vue": "cd packages/vue && bun"
  }
}
